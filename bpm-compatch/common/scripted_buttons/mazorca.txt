
mazorca_button = {
	name = mazorca_button
	desc = mazorca_button_desc

	visible = { always = yes }
	cooldown = { days = normal_modifier_time }
	possible = {
		c:ARG ?= this # important because c:ARG can not exist

		NOT = {
			has_modifier = mazorca_modifier
		}

		OR = {
			has_law = law_type:law_autocracy
			has_law = law_type:law_military_junta
			has_law = law_type:law_oligarchy
		}
		has_law = law_type:law_local_police #Mazorca

		OR = {
			country_turmoil > 0.10
			radical_population_fraction > 0.10
			has_revolution = yes
		}
	}

	effect = {
		trigger_event = mazorca.2

	}

	ai_chance = {
		value = 10
	}
}
